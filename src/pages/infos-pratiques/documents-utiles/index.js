import React from 'react';
import { NavContainer } from '../../../components';

const DocumentsUtiles = ({ ...props }) => {
  const { location } = props;
  return <NavContainer location={location}>DocumentsUtiles</NavContainer>;
};

export default DocumentsUtiles;
