import React from 'react';
import { NavContainer } from '../../../components';

const PlanAccesGymnase = ({ ...props }) => {
  const { location } = props;
  return <NavContainer location={location}>PlanAccesGymnase</NavContainer>;
};

export default PlanAccesGymnase;
