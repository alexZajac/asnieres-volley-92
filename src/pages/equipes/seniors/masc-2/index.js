import React from 'react';
import { NavContainer } from '../../../../components';

const Main2Masc = ({ ...props }) => {
  const { location } = props;
  return <NavContainer location={location}>Main2Masc</NavContainer>;
};

export default Main2Masc;
