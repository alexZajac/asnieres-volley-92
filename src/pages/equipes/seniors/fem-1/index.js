import React from 'react';
import { NavContainer } from '../../../../components';

const Main1Fem = ({ ...props }) => {
  const { location } = props;
  return <NavContainer location={location}>Main1Fem</NavContainer>;
};

export default Main1Fem;
