import React from 'react';
import { NavContainer } from '../../../../components';

const M15 = ({ ...props }) => {
  const { location } = props;
  return <NavContainer location={location}>M15</NavContainer>;
};

export default M15;
