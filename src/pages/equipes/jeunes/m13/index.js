import React from 'react';
import { NavContainer } from '../../../../components';

const M13 = ({ ...props }) => {
  const { location } = props;
  return <NavContainer location={location}>M13</NavContainer>;
};

export default M13;
