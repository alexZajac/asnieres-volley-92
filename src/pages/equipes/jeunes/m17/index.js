import React from 'react';
import { NavContainer } from '../../../../components';

const M17 = ({ ...props }) => {
  const { location } = props;
  return <NavContainer location={location}>M17</NavContainer>;
};

export default M17;
